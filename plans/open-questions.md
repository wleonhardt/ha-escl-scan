# Open questions

## Open
- Multi-scanner support: worth it, or declare `single_config_entry: true`? (views + hardcoded sensor entity_id both assume one scanner)
- Keep both card-load mechanisms (`add_extra_js_url` + lovelace resource sync) or drop one?
- Multi-document ADF concatenation (issue #1) — pypdf dependency acceptable?

## Resolved
