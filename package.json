{
  "name": "ha-escl-scan-card-tests",
  "private": true,
  "description": "Dev-only: jsdom tests for custom_components/escl_scan/static/card.js",
  "scripts": {
    "test:card": "node --check custom_components/escl_scan/static/card.js && node --test tests/card/card.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
